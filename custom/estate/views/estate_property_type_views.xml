<?xml version="1.0"?>
<odoo>

    <!-- Menu -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="estate_property_type_root" name="Settings" parent="estate_menu_root"/>
    <menuitem id="estate_property_type_menu" name="Property Types" action="estate_property_type_action" parent="estate_property_type_root"/>

    <record id="view_estate_property_type_tree" model="ir.ui.view">
        <field name="name">estate.property.type.tree</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="offer_ids"/>
                <field name="offer_count"/>
            </tree>
        </field>
    </record>

    <record id="estate_property_type_form_view" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(estate_property_offer_action)d"  
                                type="action"
                                string="Offers" 
                                class="oe_stat_button" 
                                icon="fa-money">
                            <field name="offer_count" widget="statinfo" string="Offers"/>
                        </button>
                    </div>
                    <h1><field name="name" placeholder="Enter Property Types"/></h1>
                    <div style="height: 20px"/>
                    <notebook>
                        <page string="Properties">
                            <field name="properties_ids">
                                <tree>
                                    <field name="name"/>
                                    <field name="expected_price"/>
                                    <field name="state"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>   
                    
</odoo>