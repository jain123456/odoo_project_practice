<?xml version="1.0"?>
<odoo>
    <!-- Tree View For Offers -->
    <record model="ir.ui.view" id="estate_property_offer_tree_view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree editable="top" decoration-success="status=='accepted'" decoration-danger="status=='refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <!-- <field name="status"/> -->
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accept" type="object" icon="fa-check" help="Active" attrs="{'invisible': [('status','in',('accepted','refused'))]}"/>
                <button name="action_refuse" type="object" icon="fa-times" help="Refuse" attrs="{'invisible': [('status','in',('accepted','refused'))]}"/>
                <field name="status" invisible="1"/>
            </tree>
        </field>
    </record>

    <!-- Form View For Offers -->
    <record model="ir.ui.view" id="estate_property_offer_form_view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_offer_action" model="ir.actions.act_window">
        <field name="name">Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="domain">[('property_type_id','=',active_id)]</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>
